home-budget.titerin.ovh {

    root * /var/www/html/public

    # Compression moderne
    encode zstd gzip

    # 🔥 Assets statiques prioritaires
    @assets path /build/* /storage/*
    file_server @assets

    # Laravel + FrankenPHP
    php_server

    # Bloquer fichiers cachés sensibles (.env, .git…)
    @hidden path_regexp hidden /\.(?!well-known).*
    respond @hidden 403
}
